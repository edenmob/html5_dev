$('document').ready(function() {

  // 1. Binder les évènements principaux de l'API
  // et les logguer dans la console

  // 1. Binder l'évènement dragstart (sur les objets draggable)
  // Autrement dit toutes les images de #badgesContainer
  // 2. Utiliser la méthode setData (sur l'évènement)
  // pour faire transiter l'id lors du drag and drop
  
  // 1. Binder le dragover sur la drop zone (#bigHtml5)
  // 2. Désactiver le comportement par défaut de l'évènement
  
  // 1. Binder le drop sur la drop zone (#bigHtml5)
  // 2. Mettre à jour le champ de text #displayText
  // avec la valeur que l'on fait transiter via le setData
  // en utilisant maintenant getData
});

